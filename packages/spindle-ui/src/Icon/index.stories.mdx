import { Meta, Story, Source } from '@storybook/addon-docs/blocks';
import { Clock } from './index';

# Icon

<Meta title="Icon" component={Clock} />

<Source
  language='javascript'
  code={`import ICON_NAME from '@openameba/spindle-ui/Icon/{ICON_NAME}'`}
/>

※ `ICON_NAME`はアイコンの名前です。利用したいアイコンの名前を指定してください。

## Clock Icon

<Source
  language='javascript'
  code={`import Clock from '@openameba/spindle-ui/Icon/Clock'`}
/>

<Preview withSource="open">
  <Story name="Clock Icon">
    <Clock aria-label="Clock" />
  </Story>
</Preview>

<Source
  code={`
<Clock aria-label="Clock" />
  `}
/>

## Clock Icon Overriding Color

<Source
  language='javascript'
  code={`import Clock from '@openameba/spindle-ui/Icon/Clock'`}
/>

<Preview withSource="open">
  <Story name="Clock Icon Overriding Color">
    <p style={{ color: 'green' }}>
      <Clock aria-label="Clock" />
    </p>
  </Story>
</Preview>

<Source
  code={`
<p style={{ color: 'green' }}>
  <Clock aria-label="Clock" />
</p>
  `}
/>

## Clock Icon With Size

<Source
  language='javascript'
  code={`import Clock from '@openameba/spindle-ui/Icon/Clock'`}
/>

<Preview withSource="open">
  <Story name="Clock Icon Overriding Size">
    <Clock aria-label="Clock" height="200" width="200" />
  </Story>
</Preview>

<Source
  code={`
<Clock aria-label="Clock" height="200" width="200" />
  `}
/>

## Accessibillity

アイコン単体で使われる場合は、`aria-label`を指定してください。装飾としてアイコンが利用される場合は、`aria-hidden`を`true`に指定してください。
