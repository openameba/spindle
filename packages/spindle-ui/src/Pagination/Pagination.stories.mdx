import { Description, Meta, Story, Source } from '@storybook/addon-docs/blocks';
import { actions } from '@storybook/addon-actions';
import { Pagination } from './Pagination';

# Pagination

<Meta title="Pagination" component={Pagination} />

![stability-unstable](https://img.shields.io/badge/stability-unstable-yellow.svg)

<Source
  language="javascript"
  code={`import { Pagination } from '@openameba/spindle-ui'`}
/>

<Source
  language="css"
  code={`@import './node_modules/@openameba/spindle-ui/Pagination.css'`}
/>

<Source
  language="html"
  code={`<link rel="stylesheet" href="https://unpkg.com/@openameba/spindle-ui/Pagination.css">`}
/>

<Description>Paginationはページの移動を目的として利用します。</Description>

<Description>ページの前後に移動できます。</Description>

<Description>デフォルトではアンカー要素を使用します。</Description>

<Description>
  `createUrl`に関数を渡すことでURLを任意の形で生成することが可能です。
</Description>

## 指定できるプロパティ

<Description>- `current`(必須): 現在のページ数を指定してください。</Description>
<Description>- `total`(必須): 総ページ数を指定してください。</Description>
<Description>
  - `showTotal`(任意):
  現在のページ数と総ページ数を表示します。デフォルト値はfalseです。
</Description>
<Description>
  - `onPageChange`(任意):
  リンクをクリック後に処理をしたい場合に利用できます。
</Description>
<Description>
  - `createUrl`(必須): リンクのhrefとなる値を指定してください。
</Description>

export const url = '/detail/CURRENT.html';
export const pageNumber = 1;

## current 1 total 1

<Preview withSource="open">
  <Story name="current 1 total 1">
    <Pagination
      total={1}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1 total 2

<Preview withSource="open">
  <Story name="current 1 total 2">
    <Pagination
      total={2}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={2} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 2 total 2

<Preview withSource="open">
  <Story name="current 2 total 2">
    <Pagination
      total={2}
      current={2}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={2} current={2} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1 total 3

<Preview withSource="open">
  <Story name="current 1 total 3">
    <Pagination
      total={3}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={3} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 2 total 3

<Preview withSource="open">
  <Story name="current 2 total 3">
    <Pagination
      total={3}
      current={2}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={3} current={2} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 3 total 3

<Preview withSource="open">
  <Story name="current 3 total 3">
    <Pagination
      total={3}
      current={3}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={3} current={3} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1 total 4

<Preview withSource="open">
  <Story name="current 1 total 4">
    <Pagination
      total={4}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={4} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 2 total 4

<Preview withSource="open">
  <Story name="current 2 total 4">
    <Pagination
      total={4}
      current={2}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={4} current={2} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 3 total 4

<Preview withSource="open">
  <Story name="current 3 total 4">
    <Pagination
      total={4}
      current={3}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={4} current={3} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 4 total 4

<Preview withSource="open">
  <Story name="current 4 total 4">
    <Pagination
      total={4}
      current={4}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={4} current={4} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1 total 5

<Preview withSource="open">
  <Story name="current 1 total 5">
    <Pagination
      total={5}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={5} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 2 total 5

<Preview withSource="open">
  <Story name="current 2 total 5">
    <Pagination
      total={5}
      current={2}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={5} current={2} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 3 total 5

<Preview withSource="open">
  <Story name="current 3 total 5">
    <Pagination
      total={5}
      current={3}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={5} current={3} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 4 total 5

<Preview withSource="open">
  <Story name="current 4 total 5">
    <Pagination
      total={5}
      current={4}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={5} current={4} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 5 total 5

<Preview withSource="open">
  <Story name="current 5 total 5">
    <Pagination
      total={5}
      current={5}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={5} current={5} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1 total 6

<Preview withSource="open">
  <Story name="current 1 total 6">
    <Pagination
      total={6}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={6} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 2 total 6

<Preview withSource="open">
  <Story name="current 2 total 6">
    <Pagination
      total={6}
      current={2}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={6} current={2} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 3 total 6

<Preview withSource="open">
  <Story name="current 3 total 6">
    <Pagination
      total={6}
      current={3}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={6} current={3} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 4 total 6

<Preview withSource="open">
  <Story name="current 4 total 6">
    <Pagination
      total={6}
      current={4}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={6} current={4} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 5 total 6

<Preview withSource="open">
  <Story name="current 5 total 6">
    <Pagination
      total={6}
      current={5}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={6} current={5} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 6 total 6

<Preview withSource="open">
  <Story name="current 6 total 6">
    <Pagination
      total={6}
      current={6}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={6} current={6} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1 total 7

<Preview withSource="open">
  <Story name="current 1 total 7">
    <Pagination
      total={7}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={7} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 2 total 7

<Preview withSource="open">
  <Story name="current 2 total 7">
    <Pagination
      total={7}
      current={2}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={7} current={2} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 3 total 7

<Preview withSource="open">
  <Story name="current 3 total 7">
    <Pagination
      total={7}
      current={3}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={7} current={3} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 4 total 7

<Preview withSource="open">
  <Story name="current 4 total 7">
    <Pagination
      total={7}
      current={4}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={7} current={4} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 5 total 7

<Preview withSource="open">
  <Story name="current 5 total 7">
    <Pagination
      total={7}
      current={5}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={7} current={5} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 6 total 7

<Preview withSource="open">
  <Story name="current 6 total 7">
    <Pagination
      total={7}
      current={6}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={7} current={6} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 7 total 7

<Preview withSource="open">
  <Story name="current 7 total 7">
    <Pagination
      total={7}
      current={7}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={7} current={7} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1 total 1000

<Preview withSource="open">
  <Story name="current 1 total 1000">
    <Pagination
      total={1000}
      current={1}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={1} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 2 total 1000

<Preview withSource="open">
  <Story name="current 2 total 1000">
    <Pagination
      total={1000}
      current={2}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={2} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 3 total 1000

<Preview withSource="open">
  <Story name="current 3 total 1000">
    <Pagination
      total={1000}
      current={3}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={3} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 4 total 1000

<Preview withSource="open">
  <Story name="current 4 total 1000">
    <Pagination
      total={1000}
      current={4}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={4} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 5 total 1000

<Preview withSource="open">
  <Story name="current 5 total 1000">
    <Pagination
      total={1000}
      current={5}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={5} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 200 total 1000

<Preview withSource="open">
  <Story name="current 200 total 1000">
    <Pagination
      total={1000}
      current={200}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={200} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 998 total 1000

<Preview withSource="open">
  <Story name="current 998 total 1000">
    <Pagination
      total={1000}
      current={998}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={998} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 999 total 1000

<Preview withSource="open">
  <Story name="current 999 total 1000">
    <Pagination
      total={1000}
      current={999}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={999} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## current 1000 total 1000

<Preview withSource="open">
  <Story name="current 1000 total 1000">
    <Pagination
      total={1000}
      current={1000}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={1000} current={1000} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## showTotal props true

<Description>
  `showTotal`プロパティは`true`にすることで、現在のページ数と総ページ数を表示することができます。
</Description>

<Preview withSource="open">
  <Story name="showTotal props true">
    <Pagination
      total={20}
      current={8}
      showTotal={true}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
  code={
    '<Pagination total={20} current={8} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} />'
  }
/>

## on page change function

<Description>
  各サービスでルーティングを利用したい場合は、onPageChangeの第一引数にあるeventを利用してください。
</Description>

export const handleClick = (event, pageNumber) => {
  event.preventDefault();
};

<Preview withSource="open">
  <Story name="on page change function">
    <Pagination
      total={20}
      current={8}
      createUrl={(pageNumber) => `/detail/${pageNumber}.html`}
      onPageChange={(event, pageNumber) => { handleClick(event, pageNumber); }}
      {...actions('onClick')}
    />
  </Story>
</Preview>

<Source
code={`
  const handleClick = (event, pageNumber) => {
    event.preventDefault();
    // router.push などで遷移処理を記述
};
` +'<Pagination total={20} current={8} createUrl={(pageNumber) => `/detail/${pageNumber}.html`} onPageChange={(event, pageNumber) => { handleClick(event, pageNumber); }} />'+ `
`}
/>
